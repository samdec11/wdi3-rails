= form_tag login_path, :remote => true do
  = label_tag :email
  = text_field_tag :email
  = label_tag :password
  = password_field_tag :password
  = link_to('Cancel', '#', :'data-clear-form' => true, :class => 'button round alert tiny')
  = submit_tag 'Start Flirting', :class => 'button round success tiny'