<% @planets.each do |planet| %>
<ul>
  <li>
<%= link_to(planet.name, planet_path(planet.id)) %> has <%= pluralize(planet.moons, 'moon') %>
<%= link_to('Edit', edit_path(planet.id)) %>
  </li>
  </ul>
<% end %>
<br>
<%= link_to('Add new', planets_new_path) %>